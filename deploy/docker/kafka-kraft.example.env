# Уникальный идентификатор текущего узла Kafka (в кластере должен быть уникальным для каждого брокера)
KAFKA_NODE_ID=1

# Роли, которые выполняет этот узел: broker — обработка сообщений, controller — управление метаданными
KAFKA_PROCESS_ROLES=broker,controller

# Кворум контроллеров: список ID@адрес:порт. Для одиночного брокера — просто self: 1@kafka:9093
KAFKA_CONTROLLER_QUORUM_VOTERS=1@kafka:9093

# Слушатели (адреса и порты), по которым Kafka принимает подключения:
# - PLAINTEXT — для клиентов (продюсеры, консьюмеры)
# - CONTROLLER — для внутренних целей управления кластером
KAFKA_LISTENERS=PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093

# Как клиенты будут видеть Kafka: hostname должен совпадать с именем сервиса (важно в Docker)
KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://kafka:9092

# Соответствие между именами слушателей и типами безопасности (все — PLAINTEXT для dev)
KAFKA_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT

# Указывает, какой из слушателей используется как контроллерный
KAFKA_CONTROLLER_LISTENER_NAMES=CONTROLLER

# Путь к директории, где Kafka будет хранить логи, метаданные и сообщения
KAFKA_LOG_DIRS=/var/lib/kafka/data

# Разрешить автоматическое создание топиков при первом использовании
KAFKA_AUTO_CREATE_TOPICS_ENABLE=true
